export enum APIRoute {
  Items = '/items'
}

export enum NameSpace {
  Items = 'ITEMS'
}

export enum AppRoute {
  ItemListPage = '/',
  ItemInfoPage = '/item',
  ItemEditPage = '/edit',
  ItemId = ':id'
}
